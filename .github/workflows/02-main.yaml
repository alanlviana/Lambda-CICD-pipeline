name: "02 - PROD DEPLOY"

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write
  

jobs:
  lambda-deployment:
    uses: ./.github/workflows/lambda-deployment.yaml
    with:
      environment: prod